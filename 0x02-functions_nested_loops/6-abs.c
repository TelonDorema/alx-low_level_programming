#include "main.h"

/**
 * _abs - computes the value of an int
 *
 * Description: Standard library serves a similar function
 * @r: value
 *
 * Return: always 0
 *
 */

int _abs(int r)
{
	if (r >= 0)

		return (r);
	else
		return (r * -1);
}
